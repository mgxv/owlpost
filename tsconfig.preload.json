{
    "extends": "./tsconfig.node.json",
    "compilerOptions": {
        "lib": ["ES2022", "DOM", "DOM.Iterable"],
        "types": ["node"],
        "noEmit": true
    },
    "include": ["electron/preload/**/*"],
    "exclude": ["node_modules", "dist", "dist-electron", "dist-injected", "release"]
}
